# Coverage Notes

The authoritative coverage guide is `COVERAGE.md` at the repository root.

This `docs` area stores latest coverage run information for review. Current committed summaries live under `docs/coverage/`.
